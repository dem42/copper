pub const GRAVITY: f32 = -50.0;

// note that this constant is also used in the animModelVert.glsl shader to determine the maximum number of joints
// that an animated model can have
pub const MAX_JOINTS: usize = 32;