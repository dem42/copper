language: rust
rust:
  - stable
  - beta
  - nightly
addons:
  apt:
    packages:
      - cmake
      - libxrandr-dev
      - libxinerama-dev
      - libxcursor-dev
      - libxi-dev
matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true